
   
    <form action="/result" method="GET">
        <label>Your family has been dying to wait for you. Have dinner with family.</label>
        <ul>
        {{#each dinner}}
         <li>
             <input type=radio name='options' value='{{this.name}}'/><span>{{this.name}}</span></br>
         <small> Carbonate: {{this.carbCal}}kcal, Protein: {{this.proteinCal}}kcal, Fat: {{this.fatCal}}kcal</br></dd></small></span>
         </li>
         {{/each}}
        </ul>   
         {{#each userData}}
        <button type="submit" name="name" value="{{this.Name}}">Result</button>
        {{/each}}
    </form>

  
